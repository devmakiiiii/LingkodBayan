# Dependencies allowed to run postinstall build scripts.
# pnpm 11+ requires explicit approval; values must be booleans (the placeholder
# text generated by `pnpm approve-builds` leaves installs failing with
# ERR_PNPM_IGNORED_BUILDS).
allowBuilds:
  sharp: true
  tesseract.js: true
